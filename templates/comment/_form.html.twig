{{form_start(commentForm)}}
<div class="comment-form-inputs container">
	{{form_row(commentForm.content, {'attr':{'class':'comment-content-field'}})}}
	<button type="submit" class="btn btn-primary" id="comment_save">
		Commenter
	</button>
</div>
{{form_end(commentForm)}}
